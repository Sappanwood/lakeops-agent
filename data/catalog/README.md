# Dataset catalog

This directory will contain version-controlled dataset, schema, business term,
join, KPI, freshness, sensitivity, and storage-prefix metadata. Only cataloged
logical views may be exposed to text-to-SQL.
