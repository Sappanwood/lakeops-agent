# Web

This directory will contain the React and TypeScript user interface deployed to
Azure Static Web Apps. It will present conversations, generated SQL, data sources,
pipeline health, approvals, and selected trace evidence.
